const { User } = require("../models");
const jwt = require("jsonwebtoken");

// handling register
const handleRegister = (req, res) => {
  console.log("hello");
};

module.exports = {
  handleRegister,
};
